// PascalCasing

// Make an api call to Meetup to get events 
function getEventsMeetup() {
    // Make an api call to Meetup to get events
    // Return a list of events
    
    return [];
}

function Message() {
    const name = "Evan"
    // Return <Hello World> as a React Element centered on the screen

        if (name === "Evan") {
        return (<h1 style={{textAlign: "center"}}>
            Message Component. Hello {name}!
        </h1>
        );
    } else {
           return ( <h1 style={{textAlign: "center"}}>
                Message Component. Hello World!
            </h1>
            );
        }
    
}

export default Message;